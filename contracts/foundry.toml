[profile.default]
src = "."
out = "out"
libs = ["lib", "node_modules"]
solc = "0.8.29"
optimizer = true
optimizer_runs = 200
# Inco Lightning needs the FHE precompile context; keep evm version current.
evm_version = "cancun"

[rpc_endpoints]
base_sepolia = "${BASE_SEPOLIA_RPC}"
